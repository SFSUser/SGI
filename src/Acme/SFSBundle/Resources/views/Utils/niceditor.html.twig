{# empty Twig template #}
<link href="{{asset('bundles/acmesfs/plugins/sceditor/minified/themes/default.min.css')}}" rel="stylesheet" type="text/css"/>
<script src="{{asset('bundles/acmesfs/plugins/sceditor/minified/jquery.sceditor.min.js')}}" type="text/javascript"></script>


<script type="text/javascript">
    $(function() {
        var parent = $("#{{name}}").parent();
        $("#{{name}}").appendTo($("#editor_container_{{name}}"));
        $("#{{name}}").sceditor({
            emoticons: false,
            plugins: 'xhtml',
            width: "100%",
            height: "300px",
            style: "bundles/acmesfs/plugins/sceditor/minified/jquery.sceditor.default.min.css"
        });
        $("#{{name}}").appendTo(parent)
        $("#{{form}}").submit(function(event) {
            $("#{{name}}").data("sceditor").updateOriginal();
            //$('textarea[id^=nic_editor_]').each(function() {
            //    //nicEditors.findEditor($(this).attr("id")).saveContent();
            //    $(this).data("sceditor").updateOriginal();
            //});
            return true;
        });
    });
</script>
<div id="editor_container_{{name}}">
</div>